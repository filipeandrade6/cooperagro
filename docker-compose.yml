# version: "3.7"

# services:
#   api:
#     build: .
#     ports:
#       - "8383:80"
#     env_file: "config.env"
version: "3.7"

services:
  teste-postgres-compose:
    image: postgres
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "15432:5432"
    volumes:
      - /home/filipe/code/postgres:/var/lib/postgresql/data
    networks:
      - postgres-compose-network

  teste-pgadmin-compose:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: "filipe.engenharia42@gmail.com"
      PGADMIN_DEFAULT_PASSWORD: "s3cr3t0"
    ports:
      - "16543:80"
    depends_on:
      - teste-postgres-compose
    networks:
      - postgres-compose-network

networks:
  postgres-compose-network:
    driver: bridge